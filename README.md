# Sologen: Template and link wrapper using Firebase Dynamic Links
Enable the python virtualenv: `source bin/activate`

If you haven't already, put the key into `config.yaml`: `echo 'key: "$MY_FIREBASE_KEY"' >> config.yaml`
